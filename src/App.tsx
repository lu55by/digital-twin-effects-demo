import WebGLScene from "./components/WebGLScene";

/**
 * App component
 *
 * The main application component that renders the WebGLScene.
 *
 * @returns {JSX.Element} The rendered application component
 */
function App(): JSX.Element {
  return (
    <>
      <WebGLScene />
    </>
  );
}

export default App;
